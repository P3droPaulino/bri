'use strict';

/**
 * daily-spin-setting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::daily-spin-setting.daily-spin-setting');
