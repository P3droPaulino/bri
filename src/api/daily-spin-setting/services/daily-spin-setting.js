'use strict';

/**
 * daily-spin-setting service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::daily-spin-setting.daily-spin-setting');
