'use strict';

/**
 * daily-spin-setting controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::daily-spin-setting.daily-spin-setting');
